/**
 * Firebase stub — Firebase has been replaced by SQLite backend auth.
 * This module exports no-op stubs to prevent import errors from any
 * files that still reference @/lib/firebase.
 */

export const auth = null;
export const db = null;
export const googleProvider = null;
export const facebookProvider = null;
export type Auth = null;
export type Firestore = null;